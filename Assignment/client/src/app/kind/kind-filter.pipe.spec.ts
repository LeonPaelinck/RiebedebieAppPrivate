import { KindFilterPipe } from './kind-filter.pipe';

describe('KindFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new KindFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
